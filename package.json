{
  "name": "traveeval",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Trace-to-eval control plane prototype that mines production failures and exports promptfoo-ready eval packs.",
  "scripts": {
    "seed": "node scripts/seed.mjs",
    "start": "node src/server.mjs",
    "preview": "node scripts/export-preview.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
